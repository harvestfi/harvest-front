import styled from 'styled-components'

const Container = styled.div`
  margin: auto;
  text-align: center;
`

const FakeLink = styled.span`
  text-decoration: underline;
  font-weight: bold;
`

export { Container, FakeLink }
